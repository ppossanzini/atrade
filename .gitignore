# Local transactional state (SQLite WAL): never commit runtime data.
*.db
*.db-wal
*.db-shm

# Local semantic memory (Jigen store): runtime data as well, one file per database.
*.jigen
jigendb/

# Frontend dependencies and build output.
node_modules/
dist/

# Local secrets override.
appsettings.Local.json
