---
applyTo: '**/*.cs'
---

# Backend Routing

- Use `td-backend-dev` as the backend workflow entrypoint.
- Reuse the locked stack in `docs/StackProfile.md` and update it when the C# backend is introduced.
- Keep cTrader execution, deterministic risk, and JigenDB integration outside the frontend project.
